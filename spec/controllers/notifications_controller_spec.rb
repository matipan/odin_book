require 'rails_helper'

RSpec.describe NotificationsController, type: :controller do

  describe "POST #destroy" do
    context 'redirection' do

      it 'redirects to users page with a _request_ notification' do
		pending("Don't know if I should implement a test for this")
		fail
	  end

    end
  end

end
